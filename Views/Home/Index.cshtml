@model IEnumerable<Demo_Jquery.Models.Employee>

@{
    ViewData["Title"] = "Demostración de jQuery DataTables";
}

<div class="text-center">
    <h1 class="display-4">Demostración de jQuery DataTables</h1>
    <p>Esta página muestra una tabla simple con DataTables, búsqueda integrada y un listener de eventos.</p>
</div>

<!-- Tabla HTML con datos dinámicos -->
<table id="employeesTable" class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Edad</th>
            <th>Departamento</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var employee in Model)
        {
            <tr data-id="@employee.Id">
                <td>@employee.Id</td>
                <td>@employee.Name</td>
                <td>@employee.Age</td>
                <td>@employee.Department</td>
            </tr>
        }
    </tbody>
</table>

<!-- Sección de búsqueda predictiva -->
<div class="mt-5">
    <h2>Demostración de Barra de Búsqueda Predictiva</h2>
    <p>Esta sección muestra cómo implementar una barra de búsqueda con autocompletado usando jQuery UI. Al escribir en el campo "Buscar nombre", se sugieren nombres de empleados. Al seleccionar uno, se muestran sus datos completos.</p>
    
    <div class="form-group position-relative">
        <label for="searchName">Buscar nombre:</label>
        <input type="text" id="searchName" class="form-control" placeholder="Escribe el nombre de un empleado..." autocomplete="off" />
        <ul id="suggestions" class="list-group" style="position: absolute; z-index: 1000; width: 100%; max-height: 200px; overflow-y: auto; display: none;"></ul>
    </div>
    
    <div id="employeeDetails" class="mt-3" style="display: none;">
        <h4>Datos del Empleado Seleccionado:</h4>
        <div class="card">
            <div class="card-body">
                <p><strong>ID:</strong> <span id="empId"></span></p>
                <p><strong>Nombre:</strong> <span id="empName"></span></p>
                <p><strong>Edad:</strong> <span id="empAge"></span></p>
                <p><strong>Departamento:</strong> <span id="empDept"></span></p>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            // Inicializar DataTables en la tabla con ID 'employeesTable'
            //$('#employeesTable').DataTable({
            // searching: true,
            // paging: true,
            // pageLength: 15,
            // sort: false,
            // });

            $('#employeesTable').DataTable({
                paging: true, // Habilitar paginación para dividir los datos en páginas
                searching: true, // Habilitar la barra de búsqueda integrada de DataTables
                pageLength: 5, // Mostrar 5 filas por página inicialmente
                lengthMenu: [5, 10, 25, 50], // Opciones para cambiar el número de filas por página
                language: {
                    // Configurar el idioma en español para una mejor experiencia
                    "lengthMenu": "Mostrar _MENU_ entradas por página",
                    "zeroRecords": "No se encontraron resultados",
                    "info": "Mostrando página _PAGE_ de _PAGES_",
                    "infoEmpty": "No hay registros disponibles",
                    "infoFiltered": "(filtrado de _MAX_ registros totales)",
                    "search": "Buscar:",
                    "paginate": {
                        "first": "Primero",
                        "last": "Último",
                        "next": "Siguiente",
                        "previous": "Anterior"
                    }
                }
            });

            // Listener de eventos: Al hacer clic en una fila de la tabla, mostrar un alert con el nombre del empleado
            $('#employeesTable tbody').on('click', 'tr', function () {
                var data = $('#employeesTable').DataTable().row(this).data(); // Obtener los datos de la fila clicada
                if (data) {
                    alert('Has clicado en el empleado: ' + data[1]); // data[1] es el nombre (segunda columna)
                }
            });

            // Datos de empleados para el autocompletado
            var employees = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model));

            // Utiliza un evento keyup para sugerencias manuales (sin jQuery UI)
            //$('#searchName').on('keyup', LlamarPrediccion());
            //$('#searchName').on('keyup', console.log("Value"));

            $('#searchName').on('keyup', function() {
                var term = $(this).val().trim().toLowerCase();

               // var term2 = $('#employeesTable').val().trim().toLowerCase();
                var $suggestions = $('#suggestions');

                //Cuando borro se eliminan las suggetions
                if (!term) {
                    $suggestions.empty().hide();
                    return;
                }


                //ESTA PARTE ALTAMENTE VARIABLE
                //filtrado en base  a Term
                var results = employees.filter(function(emp) {
                    var empName = (emp.name || emp.Name || '').toString();
                    return empName.toLowerCase().includes(term);
                });

                //Tenia valor anterior se borra
                $suggestions.empty();


                //pintar el resultado del mapeo
                if (results.length === 0) {
                    $('<li class="list-group-item list-group-item-secondary">No se encontraron nombres</li>').appendTo($suggestions);
                    $suggestions.show();
                    return;
                }

                console.log("Lista:" + employees);

                results.slice(0, 8).forEach(function(emp) {
                    var empName = (emp.name || emp.Name || '').toString();
                    var empId = emp.id || emp.Id || '';
                    var empAge = emp.age || emp.Age || '';
                    var empDepartment = emp.department || emp.Department || '';

                    var $item = $('<li class="list-group-item list-group-item-action" style="cursor: pointer;"></li>');
                    $item.text(empName);
                    $item.on('click', function() {
                        $('#searchName').val(empName);
                        $suggestions.empty().hide();

                        $('#empId').text(empId);
                        $('#empName').text(empName);
                        $('#empAge').text(empAge);
                        $('#empDept').text(empDepartment);
                        $('#employeeDetails').show();
                    });
                    $suggestions.append($item);
                });

                $suggestions.show();
            });

            $(document).on('click', function(e){
                if (!$(e.target).closest('#searchName, #suggestions').length) {
                    $('#suggestions').empty().hide();
                }
            });
        });
    </script>
   
}

